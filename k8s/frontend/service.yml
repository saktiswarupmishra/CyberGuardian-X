apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: sentinelai
  labels:
    app: frontend
spec:
  ports:
  - port: 3000
    targetPort: 3000
  selector:
    app: frontend
  type: ClusterIP
